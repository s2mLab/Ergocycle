
import socket            
s = socket.socket()        
port = 12345     

VECT_FORCE_UPDATE_OPCODE = 2

# This needs to be called in another thread
def start_client():
    # connect to the server on local computer
    s.connect(('computer_address', port))
    isConnected = True
    while isConnected:
         opcode = int(str(c.recv(1024), 'utf8'))
         if opcode == VECT_FORCE_UPDATE_OPCODE 
            data = struct.unpack('<10f', s.recv(1024))
            
	
	
   s.close()
